nous avons fait un premier repo parfait sans le .env 
cependant lors de la creation de celui ci nous avon fait l'erreur de commit sans save le contenu des fichier (y compris le .gitignore) donc le premier commit a pris en compte tous nos fichiers 
ainsi meme apres le save et un nouveau commit le .env file s est retrouver repertorier 
